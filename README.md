# ledger-pipeline
Dependency-free Python ETL for a messy transaction ledger — validates every row and dead-letters the bad ones with reason codes. Airflow + dbt on Databricks Free Edition planned as the orchestration and warehouse layers.
